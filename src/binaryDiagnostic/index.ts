import getParsedInput from "../../lib/inputParser";

const CHALLENGE_DIR_NAME = "binaryDiagnostic";

const input: string[] = getParsedInput({
  challengeDirName: CHALLENGE_DIR_NAME,
});

export default input;
